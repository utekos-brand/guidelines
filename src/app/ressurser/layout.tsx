import { BrandShell } from "@/components/brand-shell/brand-shell";

export default function RessurserLayout({ children }: { children: React.ReactNode }) {
  return <BrandShell>{children}</BrandShell>;
}
